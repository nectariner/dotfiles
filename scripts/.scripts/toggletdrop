#!/bin/sh

# check if the program is running first
#is_active=$(ps -aux | grep dropdown | grep -v "grep")
#
#if [ -n "$is_active" ]; then
#	echo "hello!!"
#fi

#tdrop -a -s dropdown alacritty
tdrop -w 49% -x 20 -a current
